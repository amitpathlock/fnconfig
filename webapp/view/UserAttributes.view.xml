<mvc:View
	controllerName="pl.dac.apps.fnconfig.controller.UserAttributes"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
	displayBlock="true"
	height="100%"
    xmlns:smartFilterBar="sap.ui.comp.smartfilterbar"
    xmlns:smartTable="sap.ui.comp.smarttable"
xmlns:core="sap.ui.core"
	busyIndicatorDelay="0"
>
<!-- @ui5ignore -->
<Page  >
<customHeader>
<Toolbar design="Transparent" class="plDacToolbar">
    
        <core:Icon src="sap-icon://person-placeholder" />
        <Title text="{i18n>titPIPUserAttributePage}"/>
        <ToolbarSpacer/>
        <!-- @ui5ignore -->
        	<OverflowToolbarButton type="Transparent" icon="sap-icon://full-screen" press="handleFullScreen" tooltip="Enter Full Screen Mode" visible="{viewModel>/FullScreen}"/>
			<!-- @ui5ignore -->
            <OverflowToolbarButton type="Transparent" icon="sap-icon://exit-full-screen" press="handleExitFullScreen" tooltip="Exit Full Screen Mode" visible="{viewModel>/ExitFullScreen}"/>
			
    </Toolbar>
</customHeader>
 <content>
 <!-- @ui5ignore -->
	 <smartFilterBar:SmartFilterBar
              id="smartFilterBar-ua"
               entitySet="UserAttrSet"
                persistencyKey="SmartFilterBar_Explored"
                enableBasicSearch="true"
            />
            <!-- @ui5ignore -->
            <smartTable:SmartTable
               id="idSmartTableUserAttribute"
                entitySet="UserAttrSet"
                smartFilterId="smartFilterBar-ua"
                tableType="ResponsiveTable"
                header="{i18n>titPIPSmartTableUserAttribute}"
               
                persistencyKey="SmartTableAnalytical_Explored"
                enableAutoBinding="true"
                demandPopin="true"
                class="sapUiResponsiveContentPadding"
                 placeToolbarInTable="true"
                initiallyVisibleFields="AttributeId,Description"
            >
           
         <!-- @ui5ignore -->
                <Table mode="SingleSelectLeft" id="idTableUserAttributes"
                    alternateRowColors="true"
                    sticky="ColumnHeaders,HeaderToolbar"
                    selectionChange="onTableSelectionChange"
                    growing="true"
                    growingThreshold="20"
                    updateFinished="onTableUpdateFinished"
                >
              
                    <!-- Header -->
                    <columns ></columns>
                    <!-- Data -->
                    <items>
                        <ColumnListItem >
                           <cells>
									<ObjectIdentifier title="{AttributeId}"/>
									<Text text="{Description}"/>
									
								</cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </smartTable:SmartTable>
 </content>
</Page>
</mvc:View>